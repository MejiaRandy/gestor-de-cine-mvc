<!DOCTYPE html>
<html>
<head>
    <title>Series Details</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1 class="my-4"><%= serie.name %></h1>
        <div class="card mb-4">
            <img class="card-img-top" src="<%= serie.coverImage %>" alt="<%= serie.name %>">
            <div class="card-body">
                <h5 class="card-title">Genre: <%= serie.genre %></h5>
                <div class="embed-responsive embed-responsive-16by9">
                    <iframe class="embed-responsive-item" src="<%= youtubeEmbedUrl %>" allowfullscreen></iframe>
                </div>
            </div>
        </div>
        <a href="/series" class="btn btn-secondary">Back to Series</a>
    </div>
</body>
</html>